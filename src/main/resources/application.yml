spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myLibrary
    username: spring
    password: spring123

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    enabled: true
